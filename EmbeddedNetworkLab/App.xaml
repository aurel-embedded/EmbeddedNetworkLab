<Application x:Class="EmbeddedNetworkLab.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:EmbeddedNetworkLab"
             StartupUri="UI/Shell/MainWindow.xaml">
    <Application.Resources>
        <!-- Palette centered around: 6B9080, A4C3B2, CCE3DE, EAF4F4, F6FFF8 -->
        <SolidColorBrush x:Key="PalettePrimary" Color="#6B9080" />
        <SolidColorBrush x:Key="PaletteSecondary" Color="#A4C3B2" />
        <SolidColorBrush x:Key="PaletteTertiary" Color="#CCE3DE" />
        <SolidColorBrush x:Key="PaletteQuaternary" Color="#EAF4F4" />
        <SolidColorBrush x:Key="PaletteQuinary" Color="#F6FFF8" />

        <!-- Map semantic brushes to the palette -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#6B9080" />        <!-- default button -->
        <SolidColorBrush x:Key="PrimaryLightBrush" Color="#A4C3B2" />   <!-- hover -->
        <SolidColorBrush x:Key="PrimaryDarkBrush" Color="#4F7866" />    <!-- pressed (slightly darker than primary) -->
        <SolidColorBrush x:Key="DisabledBrush" Color="#CCE3DE" />

        <!-- Navigation panel brushes (darker) -->
        <SolidColorBrush x:Key="NavBackgroundBrush" Color="#CCE3DE" />
        <SolidColorBrush x:Key="NavBorderBrush" Color="#A4C3B2" />
        
        <!-- Workspace panel brushes-->
        <SolidColorBrush x:Key="WorkspaceBackgroundBrush" Color="#F6FFF8" />

        <!-- Global Button style / template -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryLightBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryDarkBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource DisabledBrush}"/>
                                <Setter Property="Foreground" Value="#EEEEEE"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
