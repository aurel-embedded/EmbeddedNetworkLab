<UserControl x:Class="EmbeddedNetworkLab.UI.Modules.SimulatorCentrale.SimulatorCentraleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="480" d:DesignWidth="800">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header / status -->
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,12">
            <TextBlock Text="Central Simulator" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <TextBlock Text=" • " Margin="8,0" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Selection line: ports + baudrates + commands -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12" VerticalAlignment="Center">
            <!-- Port label + selection + refresh -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                <TextBlock Text="Port:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <ComboBox ItemsSource="{Binding SerialPortItems}"
                          SelectedItem="{Binding SelectedPortItem, Mode=TwoWay}"
                          DisplayMemberPath="Name"
                          Width="160"
                          IsEditable="False"
                          ToolTip="Select serial port"
                          IsEnabled="{Binding IsConfigurationEditable}">
                    <ComboBox.ItemContainerStyle>
                        <Style TargetType="ComboBoxItem">
                            <Setter Property="Foreground" Value="Black"/>
                            <Setter Property="IsEnabled" Value="True"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Present}" Value="False">
                                    <Setter Property="Foreground" Value="Gray"/>
                                    <Setter Property="IsEnabled" Value="False"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsUsable}" Value="False">
                                    <Setter Property="Foreground" Value="Gray"/>
                                    <Setter Property="IsEnabled" Value="False"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ComboBox.ItemContainerStyle>
                </ComboBox>
                <Button Content="⟳"
                        ToolTip="Refresh ports"
                        Command="{Binding RefreshPortsCommand}"
                        Width="28"
                        Height="24"
                        Margin="6,0,0,0"
                        IsEnabled="{Binding IsConfigurationEditable}"/>
            </StackPanel>

            <!-- Baudrate label + selection -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,12,0">
                <TextBlock Text="Baud rate:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <ComboBox ItemsSource="{Binding BaudRates}"
                          SelectedItem="{Binding SelectedBaud, Mode=TwoWay}"
                          Width="120"
                          IsEditable="False"
                          ToolTip="Select baud rate"
                          IsEnabled="{Binding IsConfigurationEditable}"/>
            </StackPanel>

            <!-- Single toggle command button -->
            <Button Content="{Binding ToggleLabel}" Command="{Binding ToggleCommand}" Width="100"/>
            <TextBlock Text="{Binding IsRunning, StringFormat='Running: {0}'}" Margin="12,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Main area: Commands / output -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- Commands: header row + 10 pairs Name + small TextBox + Button -->
            <GroupBox Header="Commands" Margin="0,0,12,0" Padding="6">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Column headers (compact) -->
                    <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="4,2,4,2">
                        <TextBlock Text="Command Name" Width="120" FontWeight="Bold" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Command Value" Width="180" FontWeight="Bold" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <!-- spacer for the Send button column -->
                        <TextBlock Width="80"/>
                    </StackPanel>

                    <!-- Items list (moved up directly below headers) -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="4,0,4,4" Focusable="False">
                        <ItemsControl ItemsSource="{Binding CommandItems}" Margin="0">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel IsItemsHost="True" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="0,1" VerticalAlignment="Center">
                                        <!-- Name field -->
                                        <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                 Width="120" Margin="0,0,6,0" Padding="2,2" VerticalContentAlignment="Center"/>
                                        <!-- Command text field -->
                                        <TextBox Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                 Width="180" Margin="0,0,6,0" Padding="2,2" VerticalContentAlignment="Center"/>
                                        <Button Content="Send"
                                                Command="{Binding DataContext.SendCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Width="80" Padding="2,2"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>

                            <!-- Remove default spacing between items -->
                            <ItemsControl.ItemContainerStyle>
                                <Style TargetType="ContentPresenter">
                                    <Setter Property="Margin" Value="0"/>
                                </Style>
                            </ItemsControl.ItemContainerStyle>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </GroupBox>

            <!-- Output / simulator log -->
            <GroupBox Grid.Column="1" Header="Reception" Margin="0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="41*"/>
                        <ColumnDefinition Width="23*"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Text="{Binding StatusText}" 
                             IsReadOnly="True" 
                             TextWrapping="Wrap" 
                             VerticalScrollBarVisibility="Auto"
                             Background="White" 
                             BorderThickness="0" 
                             Padding="8" Grid.ColumnSpan="2"/>
                </Grid>
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>