<UserControl x:Class="EmbeddedNetworkLab.UI.Modules.SimulatorCentrale.SimulatorCentraleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="480" d:DesignWidth="800">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- En-tête / statut -->
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,12">
            <TextBlock Text="Simulator Centrale" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <TextBlock Text=" • " Margin="8,0" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center"/>
            <TextBlock Text=" " Width="12"/>
            <TextBlock Text="{Binding StartStopLabel}" FontStyle="Italic" Foreground="Gray" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Commandes -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="Start" Command="{Binding StartCommand}" Width="100"/>
            <Button Content="Stop" Command="{Binding StopCommand}" Width="100" Margin="8,0,0,0"/>
            <Button Content="Toggle" Command="{Binding ToggleCommand}" Width="100" Margin="8,0,0,0"/>
            <TextBlock Text="{Binding IsRunning, StringFormat='Running: {0}'}" Margin="12,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Zone principale : configuration / sortie -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <!-- Configuration (placeholders si besoin d'ajouts ultérieurs) -->
            <GroupBox Header="Configuration" Margin="0,0,12,0">
                <StackPanel Margin="8">
                    <TextBlock Text="Paramètre A" />
                    <TextBox IsEnabled="True" />
                    <TextBlock Text="Paramètre B" Margin="0,8,0,0"/>
                    <TextBox IsEnabled="True"/>
                    <!-- Remplacez ou liez ces champs aux propriétés réelles si vous en ajoutez -->
                </StackPanel>
            </GroupBox>

            <!-- Sortie / journal du simulateur -->
            <GroupBox Grid.Column="1" Header="Sortie" Margin="0">
                <Grid>
                    <TextBox Text="{Binding StatusText}" 
                             IsReadOnly="True" 
                             TextWrapping="Wrap" 
                             VerticalScrollBarVisibility="Auto"
                             Background="White" 
                             BorderThickness="0" 
                             Padding="8"/>
                </Grid>
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>